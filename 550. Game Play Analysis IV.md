## Question


### Answer
```sql
with cte as (
    SELECT *, 
    LEAD(event_date, 1) OVER(PARTITION BY player_id ORDER BY event_date) as next_event_date
    FROM Activity    
)


SELECT *
FROM cte
```

## Not Finish yet
